#include "main.h"
/**
 *_islower -> checks if the character is lowercase
 * @c: the character argument
 * Return: return 1 or 0 depends on the condition
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
