#include <stdio.h>
/**
 * main- main block
 * Desciption: print single digit number
 * base 10 starting from 0
 * Return: Always 0 (Success)
 */
int main(void)
{
	char a;

	for (a = 0; a < 10 ; a++)
		putchar(a);
	putchar('\n');
	return (0);
}
